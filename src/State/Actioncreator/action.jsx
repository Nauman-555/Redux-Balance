// export const depositMoney = (amount) => {
//   return (dispatch) => {
//     dispatch({
//       type: "deposit",
//       payload: amount, // use the variable instead of the string
//     });
//   };
// };

// export const withdrawMoney = (amount) => {
//   return (dispatch) => {
//     dispatch({
//       type: "withdraw",
//       payload: amount, // use the variable instead of the string
//     });
//   };
// };
